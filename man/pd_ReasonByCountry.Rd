% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_ReasonDist.R
\name{pd_ReasonByCountry}
\alias{pd_ReasonByCountry}
\title{Premature-death reason counts by country}
\usage{
pd_ReasonByCountry(dfDeath, dfSubjects, nWindowDays = 90)
}
\arguments{
\item{dfDeath}{`data.frame` Mapped death data with `subjid`, `death_dy`, and
optionally `deathcls`.}

\item{dfSubjects}{`data.frame` Mapped subject data with `subjid` and `country`,
joined onto each death to attribute it to a country.}

\item{nWindowDays}{`numeric` Premature-death window in days. Default: 90.}
}
\value{
A named `list`: one element per country (and `"__ALL__"`), each a
  `list` with `reason`, `n`, and `hover` vectors sorted by descending `n`.
}
\description{
`r lifecycle::badge("experimental")`

Reason (`deathcls`) counts among premature deaths, split per country, plus an
`"__ALL__"` aggregate over every premature death. Powers the country-reactive
Reasons chart: the report serializes this to JSON and a client handler swaps
the bar to the clicked country's slice. Each slice is sorted by descending
count and carries a prebuilt hover string, so the client needs no arithmetic.
}
