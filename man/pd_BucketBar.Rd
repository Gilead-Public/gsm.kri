% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_BucketBar.R
\name{pd_BucketBar}
\alias{pd_BucketBar}
\title{Premature-death bucket bar chart}
\usage{
pd_BucketBar(
  dfClassified,
  nWindowDays = 90,
  strGroupCol = "studyid",
  strGroupLabel = "Group",
  strOuterCol = NULL,
  strOuterLabel = NULL,
  bRangeSlider = FALSE
)
}
\arguments{
\item{dfClassified}{`data.frame` Output of [pd_Classify()].}

\item{nWindowDays}{`numeric` Window in days (legend/color vocabulary). Default 90.}

\item{strGroupCol}{`character` Column to group by. Default "studyid".}

\item{strGroupLabel}{`character` Axis label. Default "Group".}

\item{strOuterCol}{`character` Optional parent column for a two-tier x-axis.}

\item{strOuterLabel}{`character` Optional tooltip label for the parent tier.}

\item{bRangeSlider}{`logical` Add a scroll-only x range slider. Default FALSE.}
}
\value{
A `plotly` htmlwidget.
}
\description{
`r lifecycle::badge("experimental")`

Stacked bar of [pd_Classify()] category counts per group. Each point's
`customdata` carries `[count, pct]` so the report can toggle counts/percent
client-side. On-bar `text` shows the bare count (blank for empty buckets).
Each of the five categories is its own legend entry; traces stack bottom-to-top
in `pd_DisplayOrder()` order (best outcome at the base, death-within-30 on top).
}
